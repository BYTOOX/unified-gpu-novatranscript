# Configuration du service Remote Processing
# Copiez ce fichier vers .env et remplissez les valeurs

# Token Hugging Face (requis pour Pyannote)
# Obtenez-le sur https://huggingface.co/settings/tokens
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxx

# Port du serveur (optionnel, défaut: 8000)
# PORT=8000

# Device pour Pyannote (optionnel, défaut: auto)
# - auto : GPU si disponible, fallback CPU pour GPUs AMD RDNA 3.5 problématiques
# - cpu  : forcer l'utilisation du CPU (recommandé si erreur MIOpen)
# - cuda : forcer l'utilisation du GPU
# PYANNOTE_DEVICE=auto



